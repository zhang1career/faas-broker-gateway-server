server:
  port: 18101

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/faas_broker_gateway?useSSL=false&autoReconnect=true&characterEncoding=utf8
    username: root
    password: 123123

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
